# Smoke Test — 2026-06-27 (America/Chicago)

## Terminology lock
- Canonical column: **`projection_class`** on both `projections` and `forecast_runs`.
- Migration `20260627_*` renamed `forecast_runs.forecast_class` → `projection_class`, renamed the check constraint, and rebuilt the partial unique index.
- `src/lib/forecast/lifecycle.ts` now writes and queries `projection_class`. All readers (`projections.functions.ts`, `sim.functions.ts`, `ingest.functions.ts`, `model-results.functions.ts`, `lineup-status.functions.ts`, admin UI) already use `projection_class`.
- `src/integrations/supabase/types.ts` regenerates from schema on the next typegen pass; no other code paths reference `forecast_class`.

## Pre-test DB state
```
projections (last 2d):
  legacy_unverified / superseded         15025
  legacy_unverified / legacy_unverified    557
  legacy_unverified / active               307
  preview           / superseded            50
  preview           / active                50

forecast_runs: 0 rows.
```

## Case 1 — Game without confirmed lineups: **PASS (vacuously)**
No `projection_class='official'` rows exist anywhere yet. Every public reader (`getDiamondScores`, `getSimulationLeaders`, `getTopProps`, `getConsensus`, results helpers) is hardcoded to `.eq("projection_class", "official")`, so no probabilities can render. Game tiles remain visible from the schedule loader; empty-state copy ("Awaiting confirmed lineups", "No official Diamond forecast published yet.") is wired into the new `ForecastCard` shipped in Phase 2.

## Case 2 — Generate Preview Simulations: **PASS (vacuously)**
Preview path writes `projection_class='preview'` only; the same `official`-only filter blocks them from every public reader. Admin UI surfaces previews behind the amber "Preview — not an official Diamond forecast" banner already in `_admin/admin.tsx`.

## Case 3 — Publish one eligible official forecast: **DEFERRED**
Cannot run automatically — requires an admin to click "Publish Official Forecast" on a game that has 2 confirmed SPs + both 9-deep batting orders. The current Chicago slate has no fully-eligible games (lineup-status dashboard shows 0 official published). Will be re-run by the owner from `/admin` once a slate is eligible. Record template:
```
run_id: ____
published_at: ____
model_version: alpha-0.3
sample player hit_prob: ____
sample player H.mean: ____
```

## Case 4 — Lock / live behavior: **DEFERRED (gated on Case 3)**
The lifecycle writer marks runs `locked` at first pitch and the partial unique index `(slate_date, game_pk, model_version) WHERE projection_class='official' AND status IN ('published','locked')` guarantees a single immutable official record per game. Re-runs are no-ops on locked rows.

---

Phase 2 UI ships now. Cases 3 & 4 are owner-driven verifications and will be confirmed against the live slate as soon as an eligible game qualifies.
